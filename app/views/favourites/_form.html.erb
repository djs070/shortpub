<%= simple_form_for(@favourite) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :story %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
